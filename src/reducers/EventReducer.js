


const initialState = {
    eventItems: []
}

export default function EventReducer(state = initialState, action) {
    switch (action.type) {
        case "ADD_ITEM":



        default:
            return state

    }
}